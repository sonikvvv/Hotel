package base_classes;

/**
 * {@code AS} Additional services,
 * {@code CL} Client,
 * {@code CUS} Client Used services,
 * {@code CY} Country,
 * {@code FT} Food type,
 * {@code HL} Hotel,
 * {@code RES} Reservation,
 * {@code RCT} Reservation cancel type,
 * {@code RF} Reservation form,
 * {@code RES_S} Reservation status,
 * {@code RES_T} Reservation type,
 * {@code ROOM} Room,
 * {@code RM_S} Room status,
 * {@code RM_T} Room type,
 * {@code U} User,
 * {@code UR} User Roel,
 * {@code NULL}
 */
public enum ClassesE {
    AS,
    CL,
    CUS,
    CY,
    FT,
    HL,
    RES,
    RCT,
    RF,
    RES_S,
    RES_T,
    ROOM,
    RM_S,
    RM_T,
    U,
    UR,
    NULL
}
